#!/usr/bin/env python

import sys

in_file = sys.argv[1]
cutoff = sys.argv[2]
file = open(in_file, "r")
line_nr = 1
exluded_sites = []

for line in file:
	if line < cutoff
	exluded_sites.append(line)
print excluded_sites	
